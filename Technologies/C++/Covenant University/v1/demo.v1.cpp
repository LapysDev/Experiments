/* --- NOTE ---
    #Lapys:
        - Creating Variables
            -- [data type] [variable name] = [value];

        - Creating Functions
            -- [data type] [function name]() {}

        - Data Types
            -- auto, bool, char, double, float, int, std::string, void

            -- auto variable;
            -- char character = 'a';
            -- int integer = 1;
            -- string myString = "Hello";
            -- std::string stillMyString = "World";
            -- void function() {};

        - Namespaces are objects, but their properties are accessed with `::`.

        - iostream == Input-Output Stream
        - std == Standard Library (which is a namespace)
*/
#include <iostream>
using namespace std;
int main() {
    std::cout << "Hello, World!";
    return 0;
}
